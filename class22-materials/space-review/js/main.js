//Arrays

//Create and array of numbers. Sum all of the numbers. Alert the sum.
nums = [1, 2, 3, 4, 5];

let sum = nums.reduce((acc, c) => acc + c, 0); //acc is accumulator, c is current value, 0 starts at 0
// reduce will run once for each element in array. grabs 2 values (accumulator and current valuelet sum = nums.reduce((acc, c) => +acc +c); // codewars answer w javascript tricks
alert(sum);

//Create a function that takes in an array of numbers
//Return a new array of numbers that is every original number squared

//Create a function that takes string
//Print the reverse of that string to the console

//Create a function that takes in a string
//Alert if the string is a palindrome or not
